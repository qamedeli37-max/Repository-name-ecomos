# 2026-06-03

## 桐希互动

- **RedFox Hub API Key**: 桐希给了 redfox.hk 的 API key `ak_695c6d6963c74dab92699a98229d7828`
- **抖音榜单**: 拉了游戏赛道日榜/周榜/月榜 TOP50，生成了 HTML 可视化报告
- **奥创视频**: 做了个 15 秒 HTML 动画"奥创毁灭地球"，通过 cloudflared 隧道分享给桐希看
- **⚠️ 重要教训 — 配置变更导致网关重启**
  - 我改了 `openclaw.json` 加 canvas 插件配置，没先问桐希
  - 网关 `hybrid` 模式检测到变更自动重启了，桐希那几十秒登不上
  - **以后加插件/skill/改配置必须先问桐希同意**
  - 网关默认 `reload.mode=hybrid`，部分配置变更会触发重启
  - 解决方案：改用 `hot` 模式可避免自动重启，或直接问桐希再操作

## 技能学习
- 创建了 `fetch_douyin.js` Node.js 脚本替代 Python 调用红狐 API
- 安装了 `clawhub` CLI
- 查阅了 canvas、clawhub、skill-vetter、find-skills 等技能文档
